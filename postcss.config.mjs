const config = {
  // Tailwind CSS v4: use the official PostCSS plugin
  // Vite expects plugin instances or a plugins object map, not string names in an array.
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
